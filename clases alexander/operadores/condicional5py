#Solicitar al usuario los dos numeros

num1 = float(input("Introduce el primer numero: "))
num2 = float(input("Introduce el sengundo numero: "))
operación = input ("Intoduce la operción (+, -, *, /): ")

#Condicional paa realizar la operación
if operación == '+':
    resultado = num1 + num2
elif  operación == '-':
    resultado = num1 - num2
elif operación == '*':
    resultado = num1 * num2
elif operación == '/':
    if num2 != 0:
        resultado = "Error: División por cero"
else:
    resultado = "Operacón no valida"

print("Resultado:", resultado) 